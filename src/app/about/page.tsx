import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'About Page',
};

const About = () => {
  return (
    <div>
      <h1>About Page</h1>
    </div>
  );
};

export default About;
